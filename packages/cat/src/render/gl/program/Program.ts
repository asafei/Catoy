/** @format */

export interface IProgramConf {
    depthTest: boolean //gl.DEPTH_TEST
    depthFunc: GLenum
    depthMask: boolean
    clearColor: number[]
    clearDepth: number
}

export class Program {
    // TODO:各种gl设置先放这里面
}
